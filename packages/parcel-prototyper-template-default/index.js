
/**
 * A parcel-prototyper template config. 
 * 
 * You can access the entire build config through `config`,
 * and can also return `scripts` as an object of NPM scripts,
 * and `dependencies` as an array of valid NPM dependencies.
 */
module.exports = (config) => {
    return {}
}